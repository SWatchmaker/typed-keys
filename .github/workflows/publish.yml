name: Publish to npm

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: main
    env:
      NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Detect version change
        id: version
        run: |
          OLD_VERSION=$(git show "${{ github.event.before }}":package.json | jq -r .version)
          NEW_VERSION=$(jq -r .version < package.json)
          if [ "$OLD_VERSION" = "$NEW_VERSION" ]; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
            echo "version=$NEW_VERSION" >> "$GITHUB_OUTPUT"
          fi

      - name: Verify NPM_TOKEN secret
        if: steps.version.outputs.changed == 'true'
        run: |
          if [ -z "$NODE_AUTH_TOKEN" ]; then
            echo "::error::NPM_TOKEN secret is not set or empty"
            exit 1
          fi
          echo "NPM_TOKEN is configured (length: ${#NODE_AUTH_TOKEN})"

      - uses: pnpm/action-setup@v4
        if: steps.version.outputs.changed == 'true'
        with:
          version: 10

      - uses: actions/setup-node@v4
        if: steps.version.outputs.changed == 'true'
        with:
          node-version: 22
          cache: pnpm
          registry-url: https://registry.npmjs.org

      - run: pnpm install
        if: steps.version.outputs.changed == 'true'

      - run: pnpm run build
        if: steps.version.outputs.changed == 'true'

      - run: pnpm run test
        if: steps.version.outputs.changed == 'true'

      - name: Create git tag
        if: steps.version.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag "v${{ steps.version.outputs.version }}"
          git push origin "v${{ steps.version.outputs.version }}"

      - run: pnpm publish --access public --no-git-checks --ignore-scripts
        if: steps.version.outputs.changed == 'true'
